#include "main.h"

/**
 * -abs - computers the absolute value of an integer
 *  @c: the integer to be computed
 *  Return: the absolute value
 */
int _abs(int)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
